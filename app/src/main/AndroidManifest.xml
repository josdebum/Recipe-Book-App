<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipebook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgetPasswordActivity"></activity>
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".FavedRecipe" />
        <activity android:name=".Recipe1" />
        <activity android:name=".Network" />
        <activity android:name=".Friends" />
        <activity android:name=".Favourites" />
        <activity android:name=".Chat" />
        <activity android:name=".OpenedChat" />
        <activity android:name=".Profile" />
        <activity android:name=".UnfollowFriends" />
        <activity android:name=".Home" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>